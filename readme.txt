Instructions: To run the program, enter the 'main' folder and compile the program with the 'make' or 'make -f Makefile' command. 
Once it has compiled, you can run the program using the ‘./cg05 vs.glsl frag.glsl atlas.jpg’ command
